<h1><%= current_user.name %>さんのマイページ</h1>

<h2>今までの投稿一覧 <%= current_user.posts.count %>件</h2>
<% current_user.posts.each do |post| %>
  <p><%= link_to post.title, post_path(post) %> | <%= post.created_at %></p>
  <br>
<% end %>